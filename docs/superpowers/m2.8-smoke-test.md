# M2.8 plugin smoke test

## Plugin validator (plugin-dev:plugin-validator subagent)

Ran after Tasks 1-8. Initial findings:
- ❌ Root `plugin.json` duplicated `.claude-plugin/plugin.json` (M1 legacy, version 0.0.1) — **removed**.
- ⚠️ `marketplace.json` used non-standard `source: {source: "git", url: ...}` object — **replaced with `source: "./"` string** (matches dj-music-plugin, exa-mcp-server convention; self-hosted marketplace).
- ⚠️ Agent missing `color:` — **added `color: cyan`**.
- ⚠️ `/cryptozavr:health` lacked `argument-hint` — **added empty hint for consistency with other commands**.
- ⚠️ `.env.example` lacked comments about allowed values — **added inline docs for `CRYPTOZAVR_MODE` (+ LOG_LEVEL)**.
- ✅ `echo` MCP tool confirmed registered in `src/cryptozavr/mcp/server.py::_register_echo` — `/cryptozavr:health` works.
- ✅ All 4 slash commands, agent, 2 skills, hook, cross-platform wrappers valid.

Final validator pass: PASS.

## Local marketplace install (user action)

To verify in a fresh Claude Code session (NOT this agent session):
```text
/plugin marketplace add /Users/laptop/dev/cryptozavr
/plugin install cryptozavr@cryptozavr-marketplace
```

Expected:
- Plugin loads without errors
- SessionStart banner prints on new conversation: "cryptozavr plugin loaded"
- Slash commands visible under `/cryptozavr:*`
- MCP tools registered: `echo`, `get_ticker`, `get_ohlcv`, `get_order_book`, `get_trades`

## End-to-end

After install + `.env` populated (see main README):
```text
/cryptozavr:health
# expects: ✅ MCP server reachable + version + tool list

/cryptozavr:ticker kucoin BTC-USDT
# expects: price + reason_codes ["venue:healthy", "symbol:found", "cache:hit" OR "cache:miss"+"provider:called"]

/cryptozavr:research kucoin BTC-USDT
# expects: 4-tool parallel collage (Price → Trend → Liquidity → Flow → Provenance)
```

## Remote marketplace install (after push to GitHub)

Once `v0.1.0` tag is pushed:
```text
/plugin marketplace add https://github.com/evgenygurin/cryptozavr
/plugin install cryptozavr@cryptozavr-marketplace
```

Same verification steps.
